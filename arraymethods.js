var shoppingList = ['apples', 'pizza', 'chicken', 'mushrooms', 'oranges', 'beef'];

shoppingList.push('grapes');
shoppingList.unshift('crackers');
shoppingList.shift();
shoppingList.pop();

//pop and push are for the end
//shift and unshift are for the beginning